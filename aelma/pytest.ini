[pytest]
# Dotted test filenames (llm_narrator.test.py, schemas.test.py) are not
# importable under pytest's default "prepend" import mode; importlib mode
# loads them directly from their path.
addopts = --import-mode=importlib
